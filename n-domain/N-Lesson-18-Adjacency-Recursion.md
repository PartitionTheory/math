# N‑Lesson‑18 — Adjacency Recursion

Adjacency recursion is the first operator in the ABR structural engine that recursively expands 
adjacency itself. It marks the transition from synthetic recursion (Rₛ) into structural adjacency 
calculus — the bridge between Phoenix Epoch‑8 and the early structures of Epoch‑9.

Adjacency recursion is not graph traversal, not symbolic recursion, and not arithmetic recursion. 
It is structural recursion driven by:
- Δ (Partition Density),
- Ω (Structural Ordering),
- ×ₛ (Structural Multiplication),
- !ₛ (Structural Factorial),
- Πₛ and Cₛ (Structural Combinatorial Operators),
- Rₛ (Synthetic Recursion).

Adjacency recursion allows ABR to recursively expand adjacency chains and generate adjacency‑based 
recursion trees.

---

## 1. Motivation

Human adjacency is defined by graph edges:
node → neighbor

Human recursion is defined by repeated function calls:
f(n) = f(n−1)

ABR does not assume:
- numeric decrement,
- graph edges,
- symbolic functions,
- call stacks.

ABR requires recursion that respects:
- adjacency (Ω),
- density (Δ),
- structural displacement (×ₛ),
- factorial recursion (!ₛ),
- combinatorial influence (Πₛ, Cₛ),
- synthetic recursion (Rₛ).

Adjacency recursion provides this.

---

## 2. Definition of Adjacency Recursion (Aₛ)

Let N be a structural domain, and let p be a partition index.

Definition:

Aₛ(p) = Rₛ(p) ∘ Ω(p) ∘ Ω^{Δ(p)}(p)

Where:
- Rₛ applies synthetic recursion,
- Ω(p) expands adjacency by one step,
- Ω^{Δ(p)}(p) expands adjacency by Δ(p) steps,
- “∘” denotes structural composition.

In words:
Adjacency recursion is synthetic recursion applied to adjacency expansion, driven by density and ordering.

---

## 3. Properties of Aₛ

### 3.1 Non‑Arithmetic
Aₛ does not compute numeric recursion or graph traversal.  
It computes adjacency‑driven recursive expansion.

### 3.2 Density‑Driven
Δ determines how far adjacency expands.

### 3.3 Ordering‑Driven
Ω determines adjacency direction.

### 3.4 Synthetic
Rₛ determines recursive combinatorial influence.

### 3.5 Domain‑Dependent
Aₛ depends entirely on the N‑domain.

### 3.6 Recursive
Aₛ uses adjacency to recursively expand adjacency.

---

## 4. Interpretation

Aₛ answers:

“How does partition p recursively expand its adjacency chain through synthetic recursion, with 
expansion depth determined by its density?”

This is the structural analogue of adjacency recursion.

---

## 5. Conceptual Example

If Δ(p) = 3:

Aₛ(p) = Rₛ(p) ∘ Ω(p) ∘ Ω³(p)

This represents:
- synthetic recursion through Rₛ,
- adjacency expansion through Ω,
- density‑driven depth through Δ.

Aₛ is recursive adjacency expansion, not graph traversal.

---

## 6. Aₛ in the ABR Engine

With Aₛ defined, ABR gains:
- adjacency recursion,
- adjacency‑driven recursion trees,
- recursive adjacency graphs,
- density‑driven adjacency expansion,
- the foundation for structural recursion trees,
- the first adjacency operator of Phoenix Epoch‑9.

Aₛ completes the Δ → Ω → ×ₛ → !ₛ → Πₛ/Cₛ → Rₛ → Aₛ progression.

---

## 7. Forward Path

Future lessons may introduce:
- structural recursion trees,
- adjacency graph calculus,
- Phoenix Epoch‑9 structural recursion theory,
- structural fixed‑point operators.

---

## 8. Summary

Adjacency recursion (Aₛ) is now part of the ABR mathematical engine. It formalizes recursive 
adjacency expansion within an N‑domain. Aₛ is synthetic, density‑driven, adjacency‑based, and 
domain‑dependent.

N‑Lesson‑18 stands as the canonical definition of adjacency recursion.

