# N‑Lesson‑87 — Epoch Flow Potential Conformal Curl Operator PCCurl_s

## 1. From Conformal Divergence to Conformal Curl

In N‑Lesson‑86 we introduced the conformal divergence operator:

PCDiv_s(E) = $\text{div}(PCGrad_s(E)) + \frac{1}{n-2} PCSc_s(E)$

which measures conformal expansion and contraction of recursion potential.

Now we introduce the epoch flow potential conformal curl operator:

PCCurl_s

The conformal curl operator measures **rotational behavior** of recursion potential under conformal geometry. It is the conformal analogue of the standard curl but weighted by conformal curvature intensity.

---

## 2. Defining the Epoch Flow Potential Conformal Curl Operator

Let PCGrad_s be the conformal gradient operator and PCSc_s the conformal scalar operator.

Define the conformal curl operator as:

PCCurl_s(E)
= $\text{curl}(PCGrad_s(E)) + \frac{1}{n-2} PCSc_s(E)$

Interpretation:

- PCCurl_s(E) > 0 → conformal rotational tendency  
- PCCurl_s(E) < 0 → inverse conformal rotation  
- PCCurl_s(E) = 0 → conformally irrotational epoch  

PCCurl_s captures rotational behavior shaped by conformal curvature rather than full metric geometry.

---

## 3. Conformal Rotational Behavior of Recursion Potential

The conformal curl operator reveals:

- how attractor influence creates conformal rotational wells  
- how repeller influence creates conformal rotational peaks  
- how recursion tendency twists under conformal curvature  
- how structural features induce conformal rotational channels  
- how recursion transitions between conformally irrotational and rotational regimes  

Epochs with high $\|PCCurl_s\|$ exhibit strong conformal twisting dynamics.

---

## 4. Conformal Curl Basins

Define the conformal curl basin of epoch E:

$B_{PCCurl}(E)
=\{\,E' \in \mathcal{E} \mid
\text{sign}(|PCCurl_s(E')|)
=\text{sign}(|PCCurl_s(E)|)
\,\}$

This partitions the epoch domain into:

- conformal rotational basins  
- inverse conformal rotational basins  
- conformally irrotational basins  

These refine the conformal divergence basins introduced in N‑Lesson‑86.

---

## 5. Epoch Conformal Curl Density ΔPCCurl

We define the epoch conformal curl density:

ΔPCCurl

### Global conformal curl density

$\Delta PCCurl_{global}
=\dfrac{\text{measure}(PCCurl_s)}{\text{measure}(M_s)}$

### Local conformal curl density

For any region R ⊆ N:

$\Delta PCCurl(R)
=\dfrac{\text{measure}(PCCurl_s \cap M_s(R))}
        {\text{measure}(M_s(R))}$

This reveals where conformal rotational behavior concentrates across the epoch manifold.

---

## 6. Relationship Between All Prior Operators and PCCurl_s

The analytic hierarchy now includes:

- all flow-analytic operators  
- all potential-analytic operators  
- all conformal-analytic operators  
- and now:  
- PCCurl_s — conformal curl operator

Compactly:

PCCurl_s = $\text{curl}(PCGrad_s) + \frac{1}{n-2} PCSc_s$

The conformal curl operator is the conformal-rotation descriptor of recursion tendency in potential-space.

---

## 7. PCCurl_s and Synthetic Temporal Analysis

The conformal curl operator introduces:

- conformal rotational attractor wells  
- conformal rotational repeller peaks  
- conformal twisting refinement  
- conformally warped rotational geometry  
- conformal-curl-sensitive recursion dynamics  

Epoch recursion becomes a conformal-rotation‑structured potential-analytic system.

