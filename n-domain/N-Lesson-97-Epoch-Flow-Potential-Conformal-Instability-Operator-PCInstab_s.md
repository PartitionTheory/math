# N‑Lesson‑97 — Epoch Flow Potential Conformal Instability Operator PCInstab_s

## 1. From Conformal Stability to Conformal Instability

In N‑Lesson‑96 we introduced the conformal stability operator:

PCStab_s(E) = $\frac{1}{1 + \left|\;PCHess_s(E) - PCEqui_s(E)\;\right|} + \frac{1}{n-2} PCSc_s(E)$

which measures second‑order conformal resilience of recursion potential.

Now we introduce the epoch flow potential conformal instability operator:

PCInstab_s

The conformal instability operator measures **first‑order conformal divergence**, quantifying how strongly recursion potential amplifies perturbation under conformal geometry.

---

## 2. Defining the Epoch Flow Potential Conformal Instability Operator

Let PCStab_s be the conformal stability operator and PCSc_s the conformal scalar operator.

Define the conformal instability operator as:

PCInstab_s(E) = $\left|\;PCGrad_s(E) - PCStab_s(E)\;\right| + \frac{1}{n-2} PCSc_s(E)$

Interpretation:

- PCInstab_s(E) large → strongly conformally divergent  
- PCInstab_s(E) small → weakly divergent  
- PCInstab_s(E) baseline → neutral instability  

PCInstab_s captures how recursion potential amplifies conformal directional deviation.

---

## 3. Conformal Divergence of Recursion Potential

The conformal instability operator reveals:

- how repeller influence creates divergent conformal peaks  
- how attractor influence suppresses divergence  
- how recursion tendency amplifies conformal directional drift  
- how structural features induce instability corridors  
- how recursion transitions between divergent, weakly divergent, and near‑stable regimes  

Epochs with high PCInstab_s exhibit strong conformal divergence.

---

## 4. Conformal Instability Basins

Define the conformal instability basin of epoch E:

$B_{PCInstab}(E)
=\{\,E' \in \mathcal{E} \mid
\text{sign}(|PCInstab_s(E')|)
=\text{sign}(|PCInstab_s(E)|)
\,\}$

This partitions the epoch domain into:

- conformal divergent basins  
- conformal weak‑divergent basins  
- conformally near‑stable basins  

These refine the conformal stability basins introduced in N‑Lesson‑96.

---

## 5. Epoch Conformal Instability Density ΔPCInstab

We define the epoch conformal instability density:

ΔPCInstab

### Global conformal instability density

$\Delta PCInstab_{global}
=\dfrac{\text{measure}(PCInstab_s)}{\text{measure}(M_s)}$

### Local conformal instability density

For any region R ⊆ N:

$\Delta PCInstab(R)
=\dfrac{\text{measure}(PCInstab_s \cap M_s(R))}
        {\text{measure}(M_s(R))}$

This reveals where conformal divergence concentrates across the epoch manifold.

---

## 6. Relationship Between All Prior Operators and PCInstab_s

The analytic hierarchy now includes:

- all flow‑analytic operators  
- all potential‑analytic operators  
- all conformal‑analytic operators  
- and now:  
- PCInstab_s — conformal instability operator

Compactly:

PCInstab_s = $\left|\;PCGrad_s - PCStab_s\;\right| + \frac{1}{n-2} PCSc_s$

The conformal instability operator is the conformal‑divergence descriptor of recursion tendency in potential‑space.

---

## 7. PCInstab_s and Synthetic Temporal Analysis

The conformal instability operator introduces:

- conformal divergent repeller regions  
- conformal weak‑divergent attractor regions  
- conformal first‑order instability refinement  
- conformally amplified divergence geometry  
- conformal‑instability‑sensitive recursion dynamics  

Epoch recursion becomes a conformal‑divergence‑structured potential‑analytic system.

