# N‑Lesson‑60 — Epoch Flow Potential Conservation Operator $\mathcal{CP}_s$

## 1. From Potential Invariants to Potential Conservation

In N‑Lesson‑59 we introduced the potential invariant field:

$$
\mathcal{InvP}_s=\{X\mid\nabla(X\circ\mathcal{P}_s)=0\}
$$

which identifies scalar quantities of recursion potential preserved under epoch flow.

Now we introduce the **epoch flow potential conservation operator**:

$$\mathcal{CP}_s$$

The potential conservation operator measures **global balance** of recursion potential across the manifold $\mathcal{M}_s$.

Where invariants describe persistent quantities, conservation describes **net creation or destruction** of potential.

---

## 2. Defining the Epoch Flow Potential Conservation Operator

Let $\mathcal{P}_s$ be the recursion potential field.

The potential conservation operator is defined as:

$$
\mathcal{CP}_s(E)=\nabla\cdot(\mathcal{G}_s(E))
$$

where $\mathcal{G}_s=\nabla(\mathcal{P}_s)$ is the potential gradient.

Interpretation:

- $\mathcal{CP}_s(E)=0$ → potential is globally conserved  
- $\mathcal{CP}_s(E)>0$ → net creation of potential  
- $\mathcal{CP}_s(E)<0$ → net destruction of potential  

The conservation operator captures **global balance** of recursion potential.

---

## 3. Global Balance of Recursion Potential

The potential conservation operator reveals:

- how recursion potential preserves total quantities  
- how attractor potential maintains global balance  
- how repeller potential contributes to net gain or loss  
- how potential geometry transitions between balanced and unbalanced regimes  
- how global structure persists across epochs  

Epochs with $\mathcal{CP}_s(E)=0$ exhibit perfect potential conservation.

---

## 4. Conservation Basins

Define the potential conservation basin of epoch $E$:

$$
\mathcal{B}_{\mathcal{CP}}(E)
    =\{E'\in\mathcal{E}\mid 
        \text{sign}(\mathcal{CP}_s(E'))=\text{sign}(\mathcal{CP}_s(E))
     \}
$$

This partitions the epoch domain into:

- positive conservation basins  
- negative conservation basins  
- conservation‑neutral basins  

These refine the invariant basins introduced in N‑Lesson‑59.

---

## 5. Epoch Potential Conservation Density $\Delta\mathcal{CP}$

As with earlier densities:

$\Delta\Phi$, $\Delta\Phi_G$, $\Delta\Xi$, $\Delta\rho$,  
$\Delta\Lambda$, $\Delta\Gamma$, $\Delta(\mathcal{M}_s)$,  
$\Delta V$, $\Delta\Theta$, $\Delta\kappa$, $\Delta\mathcal{L}$,  
$\Delta\mathcal{H}$, $\Delta\mathcal{J}$, $\Delta\mathcal{S}$,  
$\Delta\mathcal{T}$, $\Delta\mathcal{Sh}$, $\Delta\mathcal{A}$,  
$\Delta\mathcal{I}$, $\Delta\mathcal{Sy}$, $\Delta\mathcal{Inv}$,  
$\Delta\mathcal{C}$, $\Delta\mathcal{P}$, $\Delta\mathcal{G}$,  
$\Delta\mathcal{LP}$, $\Delta\mathcal{CurlP}$, $\Delta\mathcal{HP}$,  
$\Delta\mathcal{JP}$, $\Delta\mathcal{SP}$, $\Delta\mathcal{TorP}$,  
$\Delta\mathcal{ShP}$, $\Delta\mathcal{AP}$, $\Delta\mathcal{IP}$,  
$\Delta\mathcal{SyP}$, $\Delta\mathcal{InvP}$

we define the **epoch potential conservation density**:

$$\Delta\mathcal{CP}$$

### Global potential conservation density

$$
\Delta\mathcal{CP}_{global}
    =\frac{measure(\mathcal{CP}_s)}{measure(\mathcal{M}_s)}
$$

### Local potential conservation density

For any region $R\subseteq N$:

$$
\Delta\mathcal{CP}(R)
    =\frac{measure(\mathcal{CP}_s\cap\mathcal{M}_s(R))}
           {measure(\mathcal{M}_s(R))}
$$

This reveals where conservation behavior of recursion potential concentrates across the epoch manifold.

---

## 6. Relationship Between All Prior Operators and $\mathcal{CP}_s$

The analytic hierarchy now includes:

- $\Theta_s$ — expansion/contraction  
- $\kappa_s$ — rotation  
- $\mathcal{L}_s$ — diffusion  
- $\mathcal{H}_s$ — curvature  
- $\mathcal{J}_s$ — deformation  
- $\mathcal{S}_s$ — stability  
- $\mathcal{T}_s$ — torsion  
- $\mathcal{Sh}_s$ — shear  
- $\mathcal{A}_s$ — anisotropy  
- $\mathcal{I}_s$ — isotropy  
- $\mathcal{Sy}_s$ — symmetry  
- $\mathcal{Inv}_s$ — invariants  
- $\mathcal{C}_s$ — conservation  
- $\mathcal{P}_s$ — recursion potential  
- $\mathcal{G}_s$ — potential gradient  
- $\mathcal{LP}_s$ — potential Laplacian  
- $\mathcal{CurlP}_s$ — potential curl  
- $\mathcal{HP}_s$ — potential Hessian  
- $\mathcal{JP}_s$ — potential Jacobian  
- $\mathcal{SP}_s$ — potential stability tensor  
- $\mathcal{TorP}_s$ — potential torsion operator  
- $\mathcal{ShP}_s$ — potential shear operator  
- $\mathcal{AP}_s$ — potential anisotropy tensor  
- $\mathcal{IP}_s$ — potential isotropy operator  
- $\mathcal{SyP}_s$ — potential symmetry operator  
- $\mathcal{InvP}_s$ — potential invariant field  
- $\mathcal{CP}_s$ — potential conservation operator  

Compactly:

$$
\mathcal{CP}_s=\nabla\cdot(\mathcal{G}_s)
$$

The potential conservation operator is the **global‑balance descriptor** of recursion potential.

---

## 7. $\mathcal{CP}_s$ and Synthetic Temporal Analysis

The potential conservation operator introduces:

- global balance of attractor potential  
- global balance of repeller potential  
- conservation‑driven potential refinement  
- preserved total potential quantities  
- conservation‑analytic recursion dynamics  

Epoch recursion becomes a **globally balanced potential‑analytic system**.

---

## 8. Summary

N‑Lesson‑60 introduced the **epoch flow potential conservation operator** $\mathcal{CP}_s$:

- measures global preservation of recursion potential  
- defines conservation basins  
- introduces global/local potential conservation density $\Delta\mathcal{CP}$  
- extends the analytic hierarchy beyond potential invariants  
- provides a global‑balance lens on recursion geometry  

With $\mathcal{CP}_s$, the N‑domain gains a conservation‑analytic structure describing how recursion potential preserves or alters total quantities across temporal geometry.

