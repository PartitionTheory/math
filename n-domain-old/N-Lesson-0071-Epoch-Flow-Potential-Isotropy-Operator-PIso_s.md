# N‑Lesson‑71 — Epoch Flow Potential Isotropy Operator $\mathcal{PIso}_s$

## 1. From Potential Anisotropy to Potential Isotropy

In N‑Lesson‑70 we introduced the potential anisotropy tensor:

$$
\mathcal{PAn}_s(E)
=\mathcal{PJ}_s(E)-\frac{tr(\mathcal{PJ}_s(E))}{n}\,I
$$

which measures directional imbalance of recursion potential.

Now we introduce the **epoch flow potential isotropy operator**:

$$
\mathcal{PIso}_s
$$

The potential isotropy operator measures **directional uniformity** of recursion potential across the manifold $\mathcal{M}_s$.

Where anisotropy describes directional preference, isotropy describes **directional neutrality**.

---

## 2. Defining the Epoch Flow Potential Isotropy Operator

Let $\mathcal{PJ}_s$ be the potential Jacobian.

Define the isotropy operator as the **isotropic trace component** of the Jacobian:

$$
\mathcal{PIso}_s(E)
=\frac{tr(\mathcal{PJ}_s(E))}{n}\,I
$$

Interpretation:

- $\mathcal{PIso}_s(E)>0$ → uniform expansion of recursion potential  
- $\mathcal{PIso}_s(E)<0$ → uniform contraction of recursion potential  
- $\mathcal{PIso}_s(E)=0$ → isotropy‑neutral epoch  

The isotropy operator captures **uniform directional behavior** of recursion potential.

---

## 3. Directional Uniformity of Recursion Potential

The potential isotropy operator reveals:

- how attractor influence expands uniformly  
- how repeller influence contracts uniformly  
- how recursion tendency behaves identically across all directions  
- how structural features lose directional bias  
- how recursion transitions between anisotropic and isotropic regimes  

Epochs with high $\|\mathcal{PIso}_s\|$ exhibit strong uniform directional behavior.

---

## 4. Potential Isotropy Basins

Define the potential isotropy basin of epoch $E$:

$$
\mathcal{B}_{\mathcal{PIso}}(E)
=\{\,E' \in \mathcal{E} \mid
sign(|\mathcal{PIso}_s(E')|)
=sign(|\mathcal{PIso}_s(E)|)
\,\}
$$

This partitions the epoch domain into:

- positive isotropy basins  
- negative isotropy basins  
- isotropy‑neutral basins  

These refine the anisotropy basins introduced in N‑Lesson‑70.

---

## 5. Epoch Potential Isotropy Density $\Delta\mathcal{PIso}$

As with earlier densities:

$\Delta\Phi$, $\Delta\Phi_G$, $\Delta\Xi$, $\Delta\rho$,  
$\Delta\Lambda$, $\Delta\Gamma$, $\Delta(\mathcal{M}_s)$,  
$\Delta V$, $\Delta\Theta$, $\Delta\kappa$, $\Delta\mathcal{L}$,  
$\Delta\mathcal{H}$, $\Delta\mathcal{J}$, $\Delta\mathcal{S}$,  
$\Delta\mathcal{T}$, $\Delta\mathcal{Sh}$, $\Delta\mathcal{A}$,  
$\Delta\mathcal{I}$, $\Delta\mathcal{Sy}$, $\Delta\mathcal{Inv}$,  
$\Delta\mathcal{C}$, $\Delta\mathcal{P}$, $\Delta\mathcal{G}$,  
$\Delta\mathcal{LP}$, $\Delta\mathcal{CurlP}$, $\Delta\mathcal{HP}$,  
$\Delta\mathcal{JP}$, $\Delta\mathcal{SP}$, $\Delta\mathcal{TorP}$,  
$\Delta\mathcal{ShP}$, $\Delta\mathcal{AP}$, $\Delta\mathcal{IP}$,  
$\Delta\mathcal{SyP}$, $\Delta\mathcal{InvP}$, $\Delta\mathcal{CP}$,  
$\Delta\mathcal{PP}$, $\Delta\mathcal{PG}$, $\Delta\mathcal{PLP}$,  
$\Delta\mathcal{PCurl}$, $\Delta\mathcal{PH}$, $\Delta\mathcal{PJ}$,  
$\Delta\mathcal{PST}$, $\Delta\mathcal{PTor}$, $\Delta\mathcal{PSh}$,  
$\Delta\mathcal{PAn}$

we define the **epoch potential isotropy density**:

$$
\Delta\mathcal{PIso}
$$

### Global potential isotropy density

$$
\Delta\mathcal{PIso}_{global}
=\frac{measure(\mathcal{PIso}_s)}{measure(\mathcal{M}_s)}
$$

### Local potential isotropy density

For any region $R\subseteq N$:

$$
\Delta\mathcal{PIso}(R)
=\frac{measure(\mathcal{PIso}_s\cap\mathcal{M}_s(R))}
      {measure(\mathcal{M}_s(R))}
$$

This reveals where directional uniformity of recursion potential concentrates across the epoch manifold.

---

## 6. Relationship Between All Prior Operators and $\mathcal{PIso}_s$

The analytic hierarchy now includes:

- all flow‑analytic operators  
- all potential‑analytic operators  
- and now:  
- **$\mathcal{PIso}_s$ — potential isotropy operator**

Compactly:

$$
\mathcal{PIso}_s
=\frac{tr(\mathcal{PJ}_s)}{n}\,I
$$

The potential isotropy operator is the **directional‑uniformity descriptor** of recursion tendency in potential geometry.

---

## 7. $\mathcal{PIso}_s$ and Synthetic Temporal Analysis

The potential isotropy operator introduces:

- uniform attractor influence  
- uniform repeller influence  
- isotropy‑driven recursion refinement  
- directionally neutral potential geometry  
- isotropy‑sensitive recursion dynamics  

Epoch recursion becomes an **isotropically balanced potential‑analytic system**.

---

## 8. Summary

N‑Lesson‑71 introduced the **epoch flow potential isotropy operator** $\mathcal{PIso}_s$:

- measures directional uniformity of recursion potential  
- defines isotropy basins  
- introduces global/local potential isotropy density $\Delta\mathcal{PIso}$  
- completes the anisotropy/isotropy pair in potential‑analytic geometry  
- provides a directional‑neutral lens on recursion geometry in potential‑space  

With $\mathcal{PIso}_s$, the N‑domain gains an isotropy‑analytic structure describing how recursion potential behaves uniformly across temporal geometry.

