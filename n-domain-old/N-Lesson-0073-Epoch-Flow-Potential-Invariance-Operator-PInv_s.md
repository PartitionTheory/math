# N‑Lesson‑73 — Epoch Flow Potential Invariance Operator $\mathcal{PInv}_s$

## 1. From Potential Symmetry to Potential Invariance

In N‑Lesson‑72 we introduced the potential symmetry operator:

$$
\mathcal{PSym}_s(E)
=\mathcal{PAn}_s(E)-\mathcal{PAn}_s(E)_{break}
$$

which measures symmetry‑compatible anisotropy.

Now we introduce the **epoch flow potential invariance operator**:

$$
\mathcal{PInv}_s
$$

The potential invariance operator measures **structural invariance** of recursion potential under a chosen transformation group $G_s$.

Where symmetry describes compatibility with a group, invariance describes **complete preservation** under that group.

---

## 2. Defining the Epoch Flow Potential Invariance Operator

Let $\mathcal{PSym}_s$ be the symmetry‑compatible component of anisotropy.

Define the invariance operator as the **group‑preserving projection**:

$$
\mathcal{PInv}_s(E)
=\mathcal{PSym}_s(E)-\mathcal{PSym}_s(E)_{var}
$$

where:

- $PSym_s(E)_{var}$ is the part of symmetry‑compatible structure that **varies** under $G_s$  
- $\mathcal{PInv}_s(E)$ is the part that remains **unchanged** under $G_s$

Interpretation:

- large $\|\mathcal{PInv}_s(E)\|$ → strong invariance under $G_s$  
- $\mathcal{PInv}_s(E)=0$ → fully variant under $G_s$  

The invariance operator captures **how much of the recursion potential is preserved under a chosen transformation group**.

---

## 3. Invariance of Recursion Potential in Potential‑Space

The potential invariance operator reveals:

- how attractor influence remains unchanged under group actions  
- how repeller influence preserves invariant structure  
- how recursion tendency forms invariant manifolds  
- how structural features enforce invariance constraints  
- how recursion transitions between invariant and variant regimes  

Epochs with high $\|\mathcal{PInv}_s\|$ exhibit strong invariance‑compatible structure.

---

## 4. Potential Invariance Basins

Define the potential invariance basin of epoch $E$:

$$
\mathcal{B}_{\mathcal{PInv}}(E)
=\{\,E' \in \mathcal{E} \mid
sign(|\mathcal{PInv}_s(E')|)
=sign(|\mathcal{PInv}_s(E)|)
\,\}
$$

This partitions the epoch domain into:

- invariance‑preserving basins  
- invariance‑breaking basins  
- invariance‑neutral basins  

These refine the symmetry basins introduced in N‑Lesson‑72.

---

## 5. Epoch Potential Invariance Density $\Delta\mathcal{PInv}$

As with earlier densities:

$\Delta\Phi$, $\Delta\Phi_G$, $\Delta\Xi$, $\Delta\rho$,  
$\Delta\Lambda$, $\Delta\Gamma$, $\Delta(\mathcal{M}_s)$,  
$\Delta V$, $\Delta\Theta$, $\Delta\kappa$, $\Delta\mathcal{L}$,  
$\Delta\mathcal{H}$, $\Delta\mathcal{J}$, $\Delta\mathcal{S}$,  
$\Delta\mathcal{T}$, $\Delta\mathcal{Sh}$, $\Delta\mathcal{A}$,  
$\Delta\mathcal{I}$, $\Delta\mathcal{Sy}$, $\Delta\mathcal{Inv}$,  
$\Delta\mathcal{C}$, $\Delta\mathcal{P}$, $\Delta\mathcal{G}$,  
$\Delta\mathcal{LP}$, $\Delta\mathcal{CurlP}$, $\Delta\mathcal{HP}$,  
$\Delta\mathcal{JP}$, $\Delta\mathcal{SP}$, $\Delta\mathcal{TorP}$,  
$\Delta\mathcal{ShP}$, $\Delta\mathcal{AP}$, $\Delta\mathcal{IP}$,  
$\Delta\mathcal{SyP}$, $\Delta\mathcal{InvP}$, $\Delta\mathcal{CP}$,  
$\Delta\mathcal{PP}$, $\Delta\mathcal{PG}$, $\Delta\mathcal{PLP}$,  
$\Delta\mathcal{PCurl}$, $\Delta\mathcal{PH}$, $\Delta\mathcal{PJ}$,  
$\Delta\mathcal{PST}$, $\Delta\mathcal{PTor}$, $\Delta\mathcal{PSh}$,  
$\Delta\mathcal{PAn}$, $\Delta\mathcal{PIso}$, $\Delta\mathcal{PSym}$
  
we define the **epoch potential invariance density**:

$$
\Delta\mathcal{PInv}
$$

### Global potential invariance density

$$
\Delta\mathcal{PInv}_{global}
=\frac{measure(\mathcal{PInv}_s)}{measure(\mathcal{M}_s)}
$$

### Local potential invariance density

For any region $R\subseteq N$:

$$
\Delta\mathcal{PInv}(R)
=\frac{measure(\mathcal{PInv}_s\cap\mathcal{M}_s(R))}
      {measure(\mathcal{M}_s(R))}
$$

This reveals where invariance‑compatible recursion potential concentrates across the epoch manifold.

---

## 6. Relationship Between All Prior Operators and $\mathcal{PInv}_s$

The analytic hierarchy now includes:

- all flow‑analytic operators  
- all potential‑analytic operators  
- and now:  
- **$\mathcal{PInv}_s$ — potential invariance operator**

Compactly:

$$
\mathcal{PSym}_s
=\mathcal{PInv}_s+\mathcal{PSym}_s(E)_{var}
$$

The potential invariance operator is the **invariance‑preserving descriptor** of recursion tendency in potential geometry.

---

## 7. $\mathcal{PInv}_s$ and Synthetic Temporal Analysis

The potential invariance operator introduces:

- invariance‑preserving attractor patterns  
- invariance‑preserving repeller structures  
- group‑invariant recursion refinement  
- invariance‑aware potential geometry  
- invariance‑sensitive recursion dynamics  

Epoch recursion becomes an **invariance‑structured potential‑analytic system**.

---

## 8. Summary

N‑Lesson‑73 introduced the **epoch flow potential invariance operator** $\mathcal{PInv}_s$:

- measures structural invariance of recursion potential  
- defines invariance basins  
- introduces global/local potential invariance density $\Delta\mathcal{PInv}$  
- decomposes symmetry into invariant and variant components  
- provides an invariance‑analytic lens on recursion geometry in potential‑space  

With $\mathcal{PInv}_s$, the N‑domain gains an invariance‑analytic structure describing how recursion potential remains unchanged under transformation groups across temporal geometry.

