# N‑Lesson‑59 — Epoch Flow Potential Invariant Field $\mathcal{InvP}_s$

## 1. From Potential Symmetry to Potential Invariants

In N‑Lesson‑58 we introduced the potential symmetry operator:

$$
\mathcal{SyP}_s=\mathcal{JP}_s-\mathcal{TorP}_s
$$

which measures invariant‑preserving deformation of recursion potential.

Now we introduce the **epoch flow potential invariant field**:

$$\mathcal{InvP}_s$$

The potential invariant field identifies **scalar quantities of the recursion potential that remain unchanged** under epoch flow across the manifold $\mathcal{M}_s$.

Where symmetry describes invariant‑preserving transformations, the invariant field describes **the invariants themselves**.

---

## 2. Defining the Epoch Flow Potential Invariant Field

Let $V_s$ be the epoch flow vector field.

The potential invariant field is defined as:

$$
\mathcal{InvP}_s(E)=\{X\mid\nabla(X\circ \mathcal{P}_s(E))=0\}
$$

Interpretation:

- $\mathcal{InvP}_s(E)$ contains all scalar quantities of the potential that remain unchanged  
- invariants act as “anchors” of potential geometry  
- invariants define stable structural signatures of recursion potential  

Examples include:

- conserved potential densities  
- preserved potential curvature markers  
- stable attractor/repeller potential signatures  
- fixed structural potential invariants  

---

## 3. Structural Persistence of Recursion Potential

The potential invariant field reveals:

- which potential quantities remain unchanged across recursion  
- how attractor potential maintains persistent structure  
- how repeller potential preserves stable markers  
- how potential geometry retains long‑term features  
- how recursion transitions respect invariant constraints  

Epochs with large invariant sets exhibit strong structural persistence of potential.

---

## 4. Invariant Basins

Define the potential invariant basin of epoch $E$:

$$
\mathcal{B}_{\mathcal{InvP}}(E)
    =\{E'\in\mathcal{E}\mid 
        \mathcal{InvP}_s(E')=\mathcal{InvP}_s(E)
     \}
$$

This partitions the epoch domain into:

- identical‑invariant basins  
- partially‑shared invariant basins  
- invariant‑neutral basins  

These refine the symmetry basins introduced in N‑Lesson‑58.

---

## 5. Epoch Potential Invariant Density $\Delta\mathcal{InvP}$

As with earlier densities:

$\Delta\Phi$, $\Delta\Phi_G$, $\Delta\Xi$, $\Delta\rho$,  
$\Delta\Lambda$, $\Delta\Gamma$, $\Delta(\mathcal{M}_s)$,  
$\Delta V$, $\Delta\Theta$, $\Delta\kappa$, $\Delta\mathcal{L}$,  
$\Delta\mathcal{H}$, $\Delta\mathcal{J}$, $\Delta\mathcal{S}$,  
$\Delta\mathcal{T}$, $\Delta\mathcal{Sh}$, $\Delta\mathcal{A}$,  
$\Delta\mathcal{I}$, $\Delta\mathcal{Sy}$, $\Delta\mathcal{Inv}$,  
$\Delta\mathcal{C}$, $\Delta\mathcal{P}$, $\Delta\mathcal{G}$,  
$\Delta\mathcal{LP}$, $\Delta\mathcal{CurlP}$, $\Delta\mathcal{HP}$,  
$\Delta\mathcal{JP}$, $\Delta\mathcal{SP}$, $\Delta\mathcal{TorP}$,  
$\Delta\mathcal{ShP}$, $\Delta\mathcal{AP}$, $\Delta\mathcal{IP}$,  
$\Delta\mathcal{SyP}$

we define the **epoch potential invariant density**:

$$\Delta\mathcal{InvP}$$

### Global potential invariant density

$$
\Delta\mathcal{InvP}_{global}
    =\frac{measure(\mathcal{InvP}_s)}{measure(\mathcal{M}_s)}
$$

### Local potential invariant density

For any region $R\subseteq N$:

$$
\Delta\mathcal{InvP}(R)
    =\frac{measure(\mathcal{InvP}_s\cap\mathcal{M}_s(R))}
           {measure(\mathcal{M}_s(R))}
$$

This reveals where structural persistence of recursion potential concentrates across the epoch manifold.

---

## 6. Relationship Between All Prior Operators and $\mathcal{InvP}_s$

The analytic hierarchy now includes:

- $\Theta_s$ — expansion/contraction  
- $\kappa_s$ — rotation  
- $\mathcal{L}_s$ — diffusion  
- $\mathcal{H}_s$ — curvature  
- $\mathcal{J}_s$ — deformation  
- $\mathcal{S}_s$ — stability  
- $\mathcal{T}_s$ — torsion  
- $\mathcal{Sh}_s$ — shear  
- $\mathcal{A}_s$ — anisotropy  
- $\mathcal{I}_s$ — isotropy  
- $\mathcal{Sy}_s$ — symmetry  
- $\mathcal{Inv}_s$ — invariants  
- $\mathcal{C}_s$ — conservation  
- $\mathcal{P}_s$ — recursion potential  
- $\mathcal{G}_s$ — potential gradient  
- $\mathcal{LP}_s$ — potential Laplacian  
- $\mathcal{CurlP}_s$ — potential curl  
- $\mathcal{HP}_s$ — potential Hessian  
- $\mathcal{JP}_s$ — potential Jacobian  
- $\mathcal{SP}_s$ — potential stability tensor  
- $\mathcal{TorP}_s$ — potential torsion operator  
- $\mathcal{ShP}_s$ — potential shear operator  
- $\mathcal{AP}_s$ — potential anisotropy tensor  
- $\mathcal{IP}_s$ — potential isotropy operator  
- $\mathcal{SyP}_s$ — potential symmetry operator  
- $\mathcal{InvP}_s$ — potential invariant field  

Compactly:

$$
\mathcal{InvP}_s=\{X\mid\nabla(X\circ\mathcal{P}_s)=0\}
$$

The potential invariant field is the **structural‑persistence descriptor** of recursion potential.

---

## 7. $\mathcal{InvP}_s$ and Synthetic Temporal Analysis

The potential invariant field introduces:

- structural persistence of attractor potential  
- structural persistence of repeller potential  
- invariant‑driven potential refinement  
- long‑term potential stability  
- invariant‑analytic recursion dynamics  

Epoch recursion becomes a **structurally persistent potential‑analytic system**.

---

## 8. Summary

N‑Lesson‑59 introduced the **epoch flow potential invariant field** $\mathcal{InvP}_s$:

- identifies scalar quantities of recursion potential preserved under epoch flow  
- defines invariant basins  
- introduces global/local potential invariant density $\Delta\mathcal{InvP}$  
- extends the analytic hierarchy beyond potential symmetry  
- provides a structural‑persistence lens on recursion geometry  

With $\mathcal{InvP}_s$, the N‑domain gains an invariant‑analytic structure describing how recursion potential preserves persistent quantities across temporal geometry.

