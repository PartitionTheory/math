# N‑Lesson‑64 — Epoch Flow Potential Curl $\mathcal{PCurl}_s$

## 1. From Potential Laplacian to Potential Curl

In N‑Lesson‑63 we introduced the potential Laplacian:

$$
\mathcal{PLP}_s=\nabla\cdot(\mathcal{PG}_s)
$$

which measures second‑order diffusion of recursion tendency in potential‑space.

Now we introduce the **epoch flow potential curl**:

$$\mathcal{PCurl}_s$$

The potential curl measures **rotational circulation** of recursion potential across the potential manifold $\mathcal{M}_s$.

Where $\mathcal{PLP}_s$ describes diffusion, $\mathcal{PCurl}_s$ describes **rotation**.

---

## 2. Defining the Epoch Flow Potential Curl

Let $\mathcal{PG}_s$ be the potential gradient.

The potential curl is defined as:

$$
\mathcal{PCurl}_s(E)=\nabla\times(\mathcal{PG}_s(E))
$$

Interpretation:

- $\mathcal{PCurl}_s(E)>0$ → potential recursion tendency circulates positively  
- $\mathcal{PCurl}_s(E)<0$ → potential recursion tendency circulates negatively  
- $\mathcal{PCurl}_s(E)=0$ → curl‑neutral epoch  

The curl captures **rotational variation of recursion tendency in potential‑space**.

---

## 3. Rotational Circulation of Recursion Potential

The potential curl reveals:

- how attractor influence rotates around potential features  
- how repeller influence spirals outward  
- how recursion tendency forms vortices in potential‑space  
- how potential geometry develops rotational structures  
- how recursion transitions between clockwise and counter‑clockwise regimes  

Epochs with high $|\mathcal{PCurl}_s|$ exhibit strong rotational dynamics in potential‑space.

---

## 4. Potential Curl Basins

Define the potential curl basin of epoch $E$:

$$
\mathcal{B}_{\mathcal{PCurl}}(E)
    =\{E'\in\mathcal{E}\mid 
        \text{sign}(\mathcal{PCurl}_s(E'))=\text{sign}(\mathcal{PCurl}_s(E))
     \}
$$

This partitions the epoch domain into:

- positive curl basins  
- negative curl basins  
- curl‑neutral basins  

These refine the Laplacian basins introduced in N‑Lesson‑63.

---

## 5. Epoch Potential Curl Density $\Delta\mathcal{PCurl}$

As with earlier densities:

$\Delta\Phi$, $\Delta\Phi_G$, $\Delta\Xi$, $\Delta\rho$,  
$\Delta\Lambda$, $\Delta\Gamma$, $\Delta(\mathcal{M}_s)$,  
$\Delta V$, $\Delta\Theta$, $\Delta\kappa$, $\Delta\mathcal{L}$,  
$\Delta\mathcal{H}$, $\Delta\mathcal{J}$, $\Delta\mathcal{S}$,  
$\Delta\mathcal{T}$, $\Delta\mathcal{Sh}$, $\Delta\mathcal{A}$,  
$\Delta\mathcal{I}$, $\Delta\mathcal{Sy}$, $\Delta\mathcal{Inv}$,  
$\Delta\mathcal{C}$, $\Delta\mathcal{P}$, $\Delta\mathcal{G}$,  
$\Delta\mathcal{LP}$, $\Delta\mathcal{CurlP}$, $\Delta\mathcal{HP}$,  
$\Delta\mathcal{JP}$, $\Delta\mathcal{SP}$, $\Delta\mathcal{TorP}$,  
$\Delta\mathcal{ShP}$, $\Delta\mathcal{AP}$, $\Delta\mathcal{IP}$,  
$\Delta\mathcal{SyP}$, $\Delta\mathcal{InvP}$, $\Delta\mathcal{CP}$,  
$\Delta\mathcal{PP}$, $\Delta\mathcal{PG}$, $\Delta\mathcal{PLP}$

we define the **epoch potential curl density**:

$$\Delta\mathcal{PCurl}$$

### Global potential curl density

$$
\Delta\mathcal{PCurl}_{global}
    =\frac{measure(\mathcal{PCurl}_s)}{measure(\mathcal{M}_s)}
$$

### Local potential curl density

For any region $R\subseteq N$:

$$
\Delta\mathcal{PCurl}(R)
    =\frac{measure(\mathcal{PCurl}_s\cap\mathcal{M}_s(R))}
           {measure(\mathcal{M}_s(R))}
$$

This reveals where rotational potential behavior concentrates across the epoch manifold.

---

## 6. Relationship Between All Prior Operators and $\mathcal{PCurl}_s$

The analytic hierarchy now includes:

- all flow‑analytic operators  
- all potential‑analytic operators  
- and now:  
- **$\mathcal{PCurl}_s$ — potential curl**

Compactly:

$$
\mathcal{PCurl}_s=\nabla\times(\mathcal{PG}_s)
$$

The potential curl is the **rotational‑variation descriptor** of recursion tendency in potential geometry.

---

## 7. $\mathcal{PCurl}_s$ and Synthetic Temporal Analysis

The potential curl introduces:

- rotational attractor influence in potential‑space  
- spiral repeller dynamics in potential‑space  
- curl‑driven recursion refinement  
- vortex‑based potential geometry  
- curl‑sensitive recursion dynamics  

Epoch recursion becomes a **rotational‑diffusive potential‑analytic system**.

---

## 8. Summary

N‑Lesson‑64 introduced the **epoch flow potential curl** $\mathcal{PCurl}_s$:

- measures rotational circulation of recursion potential in potential‑space  
- defines curl basins  
- introduces global/local potential curl density $\Delta\mathcal{PCurl}$  
- extends the analytic hierarchy beyond potential Laplacians  
- provides a rotational‑variation lens on recursion geometry in potential‑space  

With $\mathcal{PCurl}_s$, the N‑domain gains a curl‑analytic structure describing how recursion tendency circulates across potential geometry.

