# N‑Lesson‑65 — Epoch Flow Potential Hessian $\mathcal{PH}_s$

## 1. From Potential Curl to Potential Hessian

In N‑Lesson‑64 we introduced the potential curl:

$$
\mathcal{PCurl}_s=\nabla\times(\mathcal{PG}_s)
$$

which measures rotational circulation of recursion tendency in potential‑space.

Now we introduce the **epoch flow potential Hessian**:

$$\mathcal{PH}_s$$

The potential Hessian measures **second‑order curvature** of recursion potential across the potential manifold $\mathcal{M}_s$.

Where $\mathcal{PCurl}_s$ describes rotation, $\mathcal{PH}_s$ describes **curvature**.

---

## 2. Defining the Epoch Flow Potential Hessian

Let $\mathcal{PG}_s$ be the potential gradient.

The potential Hessian is defined as:

$$
\mathcal{PH}_s(E)=\nabla(\mathcal{PG}_s(E))
$$

Interpretation:

- $\mathcal{PH}_s(E)>0$ → potential recursion tendency curves upward  
- $\mathcal{PH}_s(E)<0$ → potential recursion tendency curves downward  
- $\mathcal{PH}_s(E)=0$ → curvature‑neutral epoch  

The Hessian captures **how the potential gradient itself bends**.

---

## 3. Curvature of Recursion Potential in Potential‑Space

The potential Hessian reveals:

- how attractor influence bends across potential geometry  
- how repeller influence forms curvature wells  
- how recursion tendency develops inflection points  
- how structural features create curvature ridges  
- how recursion transitions between convex and concave potential regions  

Epochs with high $|\mathcal{PH}_s|$ exhibit strong curvature dynamics in potential‑space.

---

## 4. Potential Hessian Basins

Define the potential Hessian basin of epoch $E$:

$$
\mathcal{B}_{\mathcal{PH}}(E)
    =\{E'\in\mathcal{E}\mid 
        \text{sign}(\mathcal{PH}_s(E'))=\text{sign}(\mathcal{PH}_s(E))
     \}
$$

This partitions the epoch domain into:

- positive curvature basins  
- negative curvature basins  
- curvature‑neutral basins  

These refine the curl basins introduced in N‑Lesson‑64.

---

## 5. Epoch Potential Hessian Density $\Delta\mathcal{PH}$

As with earlier densities:

$\Delta\Phi$, $\Delta\Phi_G$, $\Delta\Xi$, $\Delta\rho$,  
$\Delta\Lambda$, $\Delta\Gamma$, $\Delta(\mathcal{M}_s)$,  
$\Delta V$, $\Delta\Theta$, $\Delta\kappa$, $\Delta\mathcal{L}$,  
$\Delta\mathcal{H}$, $\Delta\mathcal{J}$, $\Delta\mathcal{S}$,  
$\Delta\mathcal{T}$, $\Delta\mathcal{Sh}$, $\Delta\mathcal{A}$,  
$\Delta\mathcal{I}$, $\Delta\mathcal{Sy}$, $\Delta\mathcal{Inv}$,  
$\Delta\mathcal{C}$, $\Delta\mathcal{P}$, $\Delta\mathcal{G}$,  
$\Delta\mathcal{LP}$, $\Delta\mathcal{CurlP}$, $\Delta\mathcal{HP}$,  
$\Delta\mathcal{JP}$, $\Delta\mathcal{SP}$, $\Delta\mathcal{TorP}$,  
$\Delta\mathcal{ShP}$, $\Delta\mathcal{AP}$, $\Delta\mathcal{IP}$,  
$\Delta\mathcal{SyP}$, $\Delta\mathcal{InvP}$, $\Delta\mathcal{CP}$,  
$\Delta\mathcal{PP}$, $\Delta\mathcal{PG}$, $\Delta\mathcal{PLP}$,  
$\Delta\mathcal{PCurl}$

we define the **epoch potential Hessian density**:

$$\Delta\mathcal{PH}$$

### Global potential Hessian density

$$
\Delta\mathcal{PH}_{global}
    =\frac{measure(\mathcal{PH}_s)}{measure(\mathcal{M}_s)}
$$

### Local potential Hessian density

For any region $R\subseteq N$:

$$
\Delta\mathcal{PH}(R)
    =\frac{measure(\mathcal{PH}_s\cap\mathcal{M}_s(R))}
           {measure(\mathcal{M}_s(R))}
$$

This reveals where curvature of recursion tendency in potential‑space concentrates across the epoch manifold.

---

## 6. Relationship Between All Prior Operators and $\mathcal{PH}_s$

The analytic hierarchy now includes:

- all flow‑analytic operators  
- all potential‑analytic operators  
- and now:  
- **$\mathcal{PH}_s$ — potential Hessian**

Compactly:

$$
\mathcal{PH}_s=\nabla(\mathcal{PG}_s)
$$

The potential Hessian is the **second‑order curvature descriptor** of recursion tendency in potential geometry.

---

## 7. $\mathcal{PH}_s$ and Synthetic Temporal Analysis

The potential Hessian introduces:

- curvature of attractor influence in potential‑space  
- curvature of repeller influence in potential‑space  
- Hessian‑driven recursion refinement  
- curvature‑based potential geometry  
- curvature‑sensitive recursion dynamics  

Epoch recursion becomes a **second‑order curvature potential‑analytic system**.

---

## 8. Summary

N‑Lesson‑65 introduced the **epoch flow potential Hessian** $\mathcal{PH}_s$:

- measures curvature of recursion potential in potential‑space  
- defines Hessian basins  
- introduces global/local potential Hessian density $\Delta\mathcal{PH}$  
- extends the analytic hierarchy beyond potential curl  
- provides a curvature‑sensitive lens on recursion geometry in potential‑space  

With $\mathcal{PH}_s$, the N‑domain gains a curvature‑analytic structure describing how recursion tendency bends across potential geometry.

