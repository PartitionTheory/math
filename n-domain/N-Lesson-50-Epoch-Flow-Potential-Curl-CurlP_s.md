# N‑Lesson‑50 — Epoch Flow Potential Curl $\mathcal{CurlP}_s$

## 1. From Potential Laplacian to Potential Curl

In N‑Lesson‑49 we introduced the potential Laplacian:

$$\mathcal{LP}_s=\nabla\cdot(\mathcal{G}_s)$$

which measures second‑order diffusion of recursion potential.

Now we introduce the **epoch flow potential curl**:

$$\mathcal{CurlP}_s$$

The potential curl measures **rotational circulation** of recursion potential across the manifold $\mathcal{M}_s$.

Where $\mathcal{LP}_s$ describes diffusion, $\mathcal{CurlP}_s$ describes **rotational flow of potential**.

---

## 2. Defining the Epoch Flow Potential Curl

Let $V_s$ be the epoch flow vector field.

The potential curl is defined as:

$$\mathcal{CurlP}_s(E)=\nabla\times(\mathcal{G}_s(E))$$

Interpretation:

- $\mathcal{CurlP}_s(E)>0$ → potential circulates positively  
- $\mathcal{CurlP}_s(E)<0$ → potential circulates negatively  
- $\mathcal{CurlP}_s(E)=0$ → curl‑neutral epoch  

The curl captures **rotational variation of recursion tendency**.

---

## 3. Rotational Circulation of Recursion Potential

The potential curl reveals:

- how attractor influence rotates around structural features  
- how repeller influence spirals outward  
- how recursion tendency forms vortices  
- how flow geometry develops rotational potential fields  
- how recursion transitions between clockwise and counter‑clockwise regimes  

Epochs with high $|\mathcal{CurlP}_s|$ exhibit strong rotational dynamics.

---

## 4. Curl Basins

Define the potential curl basin of epoch $E$:

$$\mathcal{B}_{\mathcal{CurlP}}(E)=\{E'\in\mathcal{E}\mid sign(\mathcal{CurlP}_s(E'))=sign(\mathcal{CurlP}_s(E))\}$$

This partitions the epoch domain into:

- positive curl basins  
- negative curl basins  
- curl‑neutral basins  

These refine the Laplacian basins introduced in N‑Lesson‑49.

---

## 5. Epoch Potential Curl Density $\Delta\mathcal{CurlP}$

As with earlier densities:

$\Delta\Phi$, $\Delta\Phi_G$, $\Delta\Xi$, $\Delta\rho$,  
$\Delta\Lambda$, $\Delta\Gamma$, $\Delta(\mathcal{M}_s)$,  
$\Delta V$, $\Delta\Theta$, $\Delta\kappa$, $\Delta\mathcal{L}$,  
$\Delta\mathcal{H}$, $\Delta\mathcal{J}$, $\Delta\mathcal{S}$,  
$\Delta\mathcal{T}$, $\Delta\mathcal{Sh}$, $\Delta\mathcal{A}$,  
$\Delta\mathcal{I}$, $\Delta\mathcal{Sy}$, $\Delta\mathcal{Inv}$,  
$\Delta\mathcal{C}$, $\Delta\mathcal{P}$, $\Delta\mathcal{G}$, $\Delta\mathcal{LP}$

we define the **epoch potential curl density**:

$$\Delta\mathcal{CurlP}$$

### Global potential curl density

$$\Delta\mathcal{CurlP}_{global}=\frac{measure(\mathcal{CurlP}_s)}{measure(\mathcal{M}_s)}$$

### Local potential curl density

For any region $R\subseteq N$:

$$\Delta\mathcal{CurlP}(R)=\frac{measure(\mathcal{CurlP}_s\cap\mathcal{M}_s(R))}{measure(\mathcal{M}_s(R))}$$

This reveals where rotational potential behavior concentrates across the epoch manifold.

---

## 6. Relationship Between All Prior Operators and $\mathcal{CurlP}_s$

The analytic hierarchy now includes:

- $\Theta_s$ — expansion/contraction  
- $\kappa_s$ — rotation  
- $\mathcal{L}_s$ — diffusion  
- $\mathcal{H}_s$ — curvature  
- $\mathcal{J}_s$ — deformation  
- $\mathcal{S}_s$ — stability  
- $\mathcal{T}_s$ — torsion  
- $\mathcal{Sh}_s$ — shear  
- $\mathcal{A}_s$ — anisotropy  
- $\mathcal{I}_s$ — isotropy  
- $\mathcal{Sy}_s$ — symmetry  
- $\mathcal{Inv}_s$ — invariants  
- $\mathcal{C}_s$ — conservation  
- $\mathcal{P}_s$ — recursion potential  
- $\mathcal{G}_s$ — potential gradient  
- $\mathcal{LP}_s$ — potential Laplacian  
- $\mathcal{CurlP}_s$ — potential curl  

Compactly:

$$\mathcal{CurlP}_s=\nabla\times(\mathcal{G}_s)$$

The potential curl is the **rotational‑variation descriptor** of recursion tendency.

---

## 7. $\mathcal{CurlP}_s$ and Synthetic Temporal Analysis

The potential curl introduces:

- rotational attractor influence  
- spiral repeller dynamics  
- curl‑driven recursion refinement  
- vortex‑based flow geometry  
- curl‑sensitive recursion dynamics  

Epoch recursion becomes a **rotational‑diffusive geometric‑analytic system**.

---

## 8. Summary

N‑Lesson‑50 introduced the **epoch flow potential curl** $\mathcal{CurlP}_s$:

- measures rotational circulation of recursion potential  
- defines curl basins  
- introduces global/local potential curl density $\Delta\mathcal{CurlP}$  
- extends the analytic hierarchy beyond potential Laplacians  
- provides a rotational‑variation lens on recursion geometry  

With $\mathcal{CurlP}_s$, the N‑domain gains a curl‑sensitive analytic structure describing how recursion tendency circulates across temporal geometry.

